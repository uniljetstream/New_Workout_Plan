# GUI 버전 운동 자세 교정 프로그램 필수 패키지
# Python 3.8 이상 필요

# AI 모델
ultralytics>=8.0.0

# 영상 처리
opencv-python>=4.8.0

# 수치 계산
numpy>=1.24.0

# 딥러닝 프레임워크
torch>=2.0.0
torchvision>=0.15.0

# 이미지 처리 (tkinter 지원용)
pillow>=10.0.0

# 참고: tkinter는 시스템 패키지로 별도 설치 필요
# Ubuntu/Debian: sudo apt-get install python3-tk
# CentOS/RHEL: sudo yum install python3-tkinter
# macOS: 기본 포함
# Windows: 기본 포함